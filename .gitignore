# dependencies
node_modules/
**/node_modules/

# build / cache
.next/
dist/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
# key material + tokens — never commit
*.pem
*.key
*.p12
id_rsa*
.hf_token
# MCP server config can carry API keys / OAuth tokens — keep local
.mcp.json
.mcp.local.json

# test artifacts (Playwright traces/screenshots can capture testnet data)
test-results/
**/test-results/
playwright-report/
**/playwright-report/
.playwright/
coverage/

# logs
*.log

# OS + editor junk
.DS_Store
Thumbs.db
ehthumbs.db
.idea/
*.swp
*.swo
*~

# external reference repos (each has its own .git — not embedded in the Lumenia repo)
stellar-ai-guide/
stellar-build/

# tool working state / caches — local only (design-sync cache holds sample names + grades)
.stellar-build/
.stellar-new/
.design-sync/

# Vercel
.vercel

# Vercel build output (generated by build-vercel.mjs)
apps/sponsor/api/
apps/sponsor/public/

# Heavy 3D GLB models (~60M) used ONLY by the internal /brand-kit workspace, which
# is gated to a 404 in production — so they are never served to a user and were pure
# deploy + clone weight. Kept LOCAL for brand exploration; regenerate via the Meshy
# pipeline if a fresh clone needs them. (brand-kit-assets/ stays tracked — the live
# site renders those.)
apps/web/public/models/

# Local next-agent handoff (testnet keys + deploy state) — never commit
HANDOFF.md

# Personal tool/editor config — local only
.claude/
.vscode/

# Staging for the SEPARATE org `.github` repo (not part of the product repo) — local only
docs/org-github/

# Local user-test recordings (WhatsApp voice notes from live user tests) — never publish
*.opus
WhatsApp Audio*

# Rust / Soroban contract build artifacts (contracts/lumen-drop) — regenerated by cargo
target/
**/target/
*.wasm

# Internal working docs (agent manual, personal profile, strategy & planning) —
# kept local; the public repo carries the Instawards-relevant docs only
# (README, stack, PROGRESS, INSTAWARDS_SOW, EVIDENCE, ANTI_DRAIN, evidence/).
CLAUDE.md
AGENT_GUIDE.md
ARCHITECTURE.md
POSITIONING.md
OFF_RAMP_VERIFICATION.md
FRONTEND_PLAN.md
SITE_REDESIGN.md
REQUEST_MONEY.md
MASTER_PLAN.md
ROADMAP_2027.md
REVENUE_MODEL.md
FEATURES.md
SCF_INTEREST_FORM.md
RECOVERY_ARCHITECTURE.md
REVIEW_AND_ROADMAP.md
V2_SOROBAN_ESCROW.md

# Property-test (proptest) writes a snapshot per fuzz case — noise, not a fixture
contracts/lumen-drop/test_snapshots/test/no_pool_op_*.json
